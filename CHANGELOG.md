# 📝 AMAi 更新日志 (Changelog)

所有本项目的版本迭代和重要功能更新都会在此文件中记录。

---

## 🚀 [v1.1.6] - 2026-06-04

### ⚙️ 统一网站配置系统与双向同步
- **配置系统重构**：统一了后台网站配置管理机制，彻底打通了 UI 设置界面与底层 JSON/SharedPreferences 的通路。
- **配置双向同步**：实现了用户在应用内 UI 修改/添加 AI 站点配置（如 XPath、视口裁剪参数、自定义 User-Agent、注入策略等）后，自动双向同步更新至 `site_config.json` 缓存与 `SharedPreferences` 持久化中。

### ⚡ 新增剪贴板文本注入策略 (`clear_and_paste`)
- **注入策略扩展**：针对部分使用了严苛事件校验、防脚本注入机制的现代前端框架（如字节跳动豆包新版），新增了剪贴板粘贴文本注入策略 (`clear_and_paste`)。
- **机制原理**：将输入文本暂存至系统剪贴板，利用页面底层/系统的粘贴机制触发输入框的值更新，规避了直接修改 `value` 不触发事件的局限，极大增强了对复杂 AI 站点的兼容性。
- **站点配置微调**：更新了部分内置大模型站点的 XPath 与注入策略，使其注入更加稳定。

---

## 🚀 [v1.1.5] - 2026-06-02

### 🎨 应用启动图标设计精炼
- **图标细节优化**：对各平台应用图标设计进行了精细化微调，移除了图标的外部描边，并重新编译生成了适配 Android、iOS、Windows、Web 的多平台原生启动图标资源，确保各平台视觉一致与精美现代。

### 📝 演示与文档完善
- **Readme 演示补充**：在 [README.md](file:///D:/SyncFiles/Code/VScode/aaaTemp/AskMAI/README.md) 中添加了一键并发提问与视口自定义裁剪的动态演示 GIF，方便新用户快速上手。

---

## 🚀 [v1.1.4] - 2026-06-02

### 📂 项目品牌统一规范 (AMAi)
- **全局重命名**：将项目名称从 `AskMAI` 统一规范命名为 `AMAi`。更新了代码库中所有相关的类名、包名、配置文件名以及文档等，使品牌形象与代码资产保持高度一致。

---

## 🚀 [v1.1.3] - 2026-06-02

### ⚡ UI 性能与加载逻辑优化
- **WebView 刷新性能优化**：实现按需加载与显示切换，在切换显示/隐藏状态时不再刷新所有已启用的 WebView，仅更新受影响的 Tab，保留完整对话会话并大幅节约流量。
- **设置面板零延迟反馈**：优化设置面板中 AI 标签拖拽排序、删除与开启状态切换时的状态变更重绘逻辑，解决 Release 模式下 UI 刷新延迟的痛点，实现即时反馈。

### 🎨 应用图标全新升级
- **全平台图标重塑**：重新设计了应用图标，使用现代矢量图形（SVG）重新生成并替换了适配 Android、iOS、Windows、Web 各平台的原生启动图标资源，消除 Flutter 默认图标，提升产品高级感。

### 🐛 Release (AOT) 模式与指示灯优化
- **手势冲突修复**：修复了在物理设备上，横向滚动的标签栏内 `GestureDetector` 的点击事件被微小滑动手势吞没导致的切换 Tab 失败问题。使用 `Material` + `InkWell` 重构，确保 100% 灵敏的点击交互。
- **渲染冻结解决**：针对 Android 平台 WebView 启用 **Hybrid Composition** 混合渲染，解决了 Release 模式下隐藏在 `IndexedStack` 中的后台 WebView 切换到前台时发生的渲染挂起与画面冻结问题。
- **后台指示灯卡灰修复**：在 WebView 的 `onPageFinished` 原生资源加载完毕阶段强制更新为 `loaded`（绿色）状态，解决了 Android 底层对后台 WebView 的 JavaScript 节流挂起（JS Throttling）而导致的指示灯初始化卡灰问题。
- **指示灯抖动优化**：重构了 JS Observer 的 MutationObserver 触发机制，将所有状态决策统一收口至 `updateState()`。只有检测到实际的 AI 思考/生成特征（如停止按钮、思考块、流式光标等）时才会变黄闪烁，完美消除了页面资源加载完成初期 DOM 自身变动产生的黄色状态闪烁与状态抖动。
- **AOT 依赖跟踪修复**：将 `chat_screen.dart` 中复杂的 `Consumer3` 拆解重构为分别针对 `TabManagerVM` 与 `InputDistributorVM` 的局部单泛型 Consumer，并在 `initState` 中增加原生底层同步监听，彻底避开了 Dart AOT 编译器在 Release 构建时过度剪枝/去活 InheritedWidget 更新通知的 Bug。
- **虚拟导航栏干扰防护**：在物理键盘可见度计算逻辑中引入设备像素比（Device Pixel Ratio），解决 Android 高密度显示屏下系统底栏虚拟导航栏误触发“键盘已弹起”UI 隐藏逻辑的问题。

---

## 🚀 [v1.1.0] - 2026-06-01

### 📂 WebView 多模态与文件上传支持
- **功能新增**：完美支持在 WebView 窗口中调用原生系统文件选择器、调用相机拍摄及从相册选择图片。
- **权限管理**：适配 Android/iOS 底层文件访问和相机调用权限申请，彻底解决在部分大模型网页端无法上传图片、文档进行多模态交互的痛点。

### 🔄 AI 标签页拖拽排序
- **交互重构**：在设置面板的 AI 标签列表中增加了拖拽把手图标（Drag Handle），支持长按上下拖拽动态调整标签页展示顺序。
- **状态持久化**：用户拖拽排序后的顺序会被自动持久化保存至 `SharedPreferences`，并在应用重启时自动应用于顶部标签栏和多模型提问分发顺序。

### ⚙️ 全新设置抽屉 (DraggableScrollableSheet)
- **UI 优化**：重构了设置面板，采用 `DraggableScrollableSheet` 实现优雅的抽屉式拖动交互。
- **高度限制**：限制面板的最大显示高度为屏幕的 2/3，顶部拖动把手固定不随内部内容滚动。
- **便捷手势**：支持滑动内容到底部后下滑直接顺畅关闭折叠，解决了旧版设置页面撑满屏幕或在小屏设备上遮挡“编辑 AI”等弹窗的问题。

### 🚥 智能状态指示灯系统
- **状态重构**：重构了标签栏的圆形状态指示灯系统，并接入 DOM Mutation Observer 网页元素监听：
  - `灰色 (Loading)`：网页正在加载资源。
  - `绿色 (Loaded)`：网页加载完成。
  - `绿色呼吸闪烁 (Active / Thinking)`：**核心特性**。实时监听网页 DOM 树变动，当 AI 正在打字输出、搜索引擎正在检索时呼吸闪烁，使用户能直观感知后台模型的工作状态。
  - `红色 (Error)`：网页加载失败。
- **快速超时与重试**：新增 12 秒网页加载超时快速失败机制，并增加了重试遮罩层，提示一键刷新。加载失败时自动追加 VPN/代理等配置排查建议。

### ⚡ 文本注入引擎升级与策略模式
- **策略模式注入**：设计并实现了 `InjectionStrategy` 接口，解耦了文本注入和自动化提交逻辑，支持 `GenericStrategy`（普通 DOM 查找）与 `ReactFiberStrategy`（React Fiber 注入）。
- **攻克 React 锁屏问题**：针对字节跳动豆包、阿里通义千问等基于 React / Next.js / Slate.js 深度定制的虚拟 DOM 文本输入框，直接从 React Fiber 树的 Node 节点获取 React 控制器和编辑器实例，强行写入并触发更新。
- **修复重复提交**：修复了在 Qwen/ChatGPT 等网站上因为传统 `.click()` 导致发送两次或文本写入后被框架自动清空的顽疾。

### 🛡️ 手势、导航与焦点保护
- **内页后退拦截**：重构系统物理返回键逻辑。在网页内浏览时，系统返回键会被智能路由给 WebView 活跃页面用于向后导航，而不会直接退出 App 或关闭当前会话。
- **双击返回优雅退出**：在主界面实现双击物理返回键退出机制。第一次按下时会显示一个高颜值的 66% 透明度毛玻璃气泡 Toast 提示“再按一次退出”，自适应系统深色/浅色主题。
- **网页滚动焦点保护**：引入键盘焦点和可见性管理机制，彻底解决了在用户滑动/滚动 WebView 网页时，系统意外收回焦点导致软键盘频繁闪退、关闭的问题。

### ✂️ 视口剪裁体验升级
- **临时禁用视口**：在左侧操作栏增加了“临时禁用视口裁剪”功能。开启后可临时恢复标准网页视口，以便查看/操作隐藏的侧边栏或完整网页。
- **布局微调**：将视口微调对话框 (`ViewportAdjustDialog`) 启动按钮等统一移至界面左侧布局，为中间主显示区腾出了更大的纯净对话视野。

### 📦 全局配置文件与动态加载
- **静态配置提取**：引入 `assets/app_config.json` 统一管理应用全局默认配置，包括：
  - 默认主题模式（自适应/浅色/深色）
  - 标题栏是否默认显示
  - 网页加载策略（并行 / 顺序）
  - 默认启用的 AI 标签列表
  - 项目 GitHub 仓库地址
- **动态版本与关于对话框**：从 `package_info_plus` 中动态读取应用版本号（例如 `1.1.0`），点击版本号会弹出带 GitHub 链接的关于对话框，方便用户一键直达源码仓库。

---

## 🚀 [v1.0.0] - 2026-05-29

### 🎨 现代化 UI 架构与主题
- **设计规范**：实现了全新的圆形设计语言与统一主题配置（`theme_config.dart`），界面高级现代。
- **框架搭建**：使用 Flutter 平台重构应用，采用 **MVVM (Model-View-ViewModel)** 架构，结合 **Provider** 完成状态解耦与数据绑定。

### 🚀 核心自动化与视口裁剪
- **一键并发广播**：在底部全局输入框中输入问题，通过 `Future.wait` 并发调度 JavaScript 脚本向所有已开启的 WebView 执行输入及点击提交事件。
- **XPath 配置**：支持从 `assets/site_config.json` 读取和管理 AI 模型，通过 F12 浏览器控制台获取 `inputXPath` 与 `submitXPath` 即可支持自定义扩展添加任何新的大模型。
- **视口裁剪系统**：通过 `ClipRect` + `OverflowBox` 实现了对普通 AI 套壳网页的多余边框剪切与缩放，将网页对话区域完美填充至屏幕中，获得媲美原生 App 的高纯净度体验。
- **会话与备份**：自动记忆已启用的标签；支持在设置中一键导出、导入所有 AI 配置，方便跨设备迁移。
